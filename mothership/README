A web page that lists my servers. It also serves a static files from $HOME/www.

Missing features:
- authentication
- per-user data
- persistence

==
How to run on port 80:

$ go build
$ sudo setcap 'cap_net_bind_service=+ep' mothership
$ ./mothership
